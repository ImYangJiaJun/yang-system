/target/
/.idea/
/.env
/config.toml
/config.local.toml
.codex/
__pycache__/

# 部署产物：deploy/ 目录本身要入库（前端部署合同门禁会读 deploy-blue-green.sh），
# 但下面这些是**生产凭据**或临时文件，绝不能提交。
# 模板是 deploy/config.cloud.example.toml（已入库），首次部署时服务器上从它复制并填值。
deploy/config.cloud.toml
# 上面这条是精确路径，**备份/变体匹配不到**（如 config.cloud.toml.bak-20260923），
# 而它们同样含明文凭据——一次 `git add -A` 就会把生产口令提交进去（2026-09-23 实际
# 差点踩到：改注释时留下的 .bak-doc8154 里有 28 行凭据，且当时未被忽略）。
deploy/config.cloud.toml.*
deploy/.mysql-root-password
deploy/*.tar
deploy/*.tar.gz

# 飞书官方文档的本地镜像（scripts/fetch_feishu_docs.py 抓取，3574 篇 / 约 38MB）。
# 它是**可再生的参考资料**，不是源码：随时能按脚本重抓，入库只会把仓库撑大一个量级。
# 开发时按 docs/reference/feishu/README.md 使用即可。
docs/reference/feishu/

# Playwright MCP 的会话产物（截图 / 快照 / 控制台日志），不是源码。
.playwright-mcp/

# 飞书联调期的本地产物。**这些不是源码，而且多数含真实业务数据或活体凭证**：
#   · .tmp-fs/        —— 多维表格全量导出（记录逐条落盘），含真实财务台账字段
#   · *.manifest.json —— 每份导出各带一个 base_token，拿到即可读那张多维表格
#   · .tmp-*          —— 探针脚本与响应抓取（可能含 token）
#   · *.ndjson        —— 记录转储
# 已在 yang-system 仓库里实测：这 1500+ 个文件既未被跟踪、也未被忽略，
# 一次 `git add -A` 就会把它们连同 base_token 一起推到 origin。
.tmp-fs/
.tmp-*
*.manifest.json
*.ndjson

# 业务侧提供的原始表格（境内银行网点信息管理等）。是**数据**不是源码。
docs/*.xlsx
